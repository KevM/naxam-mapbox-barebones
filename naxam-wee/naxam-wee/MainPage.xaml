<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:naxamwee"
             xmlns:naxam="clr-namespace:Naxam.Controls.Mapbox.Forms;assembly=Naxam.Mapbox.Forms"
             x:Class="naxamwee.MainPage">

    <StackLayout>
        <!-- Place new controls here -->
        <Label Text="{Binding Message}" HorizontalOptions="Center" />
        <naxam:MapView 
            x:Name="naxamMap"
            Center="{Binding Center}"
            ZoomLevel="{Binding ZoomLevel}"
            UpdateViewPortAction="{Binding UpdateViewPortAction}"
            >
        </naxam:MapView>
    </StackLayout>

</ContentPage>
